# hyprpaper >= 0.8 syntax: wallpaper{} blocks; empty monitor = all monitors.
# The old preload/wallpaper lines are silently ignored (black desktop!).
wallpaper {
    monitor =
    path = ~/.config/themes/current/wallpaper.png
    fit_mode = cover
}

splash = false
