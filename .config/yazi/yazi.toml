[opener]
imv = [
    { run = 'imv "$@"', desc = "Open with imv", orphan = true, for = "unix" },
]
zathura = [
    { run = 'zathura "$@"', desc = "Open with zathura", orphan = true, for = "unix" },
]

[open]
prepend_rules = [
    { mime = "image/*", use = "imv" },
    { mime = "application/pdf", use = "zathura" },
]
